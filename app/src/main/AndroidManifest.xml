<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EinkKt"
        tools:targetApi="31">
        <activity
            android:name=".views.SendFileActivity"
            android:exported="false"
            android:label="@string/title_activity_send_file"
            android:theme="@style/Theme.EinkKt" />
        <activity
            android:name=".views.AppManagerActivity"
            android:exported="false"
            android:label="@string/title_activity_app_manager"
            android:theme="@style/Theme.EinkKt" />
        <activity
            android:name=".views.DevicesActivity"
            android:exported="false"
            android:label="@string/title_activity_devices"
            android:theme="@style/Theme.EinkKt" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EinkKt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>